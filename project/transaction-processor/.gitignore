# macOS
.DS_Store

# Environment files
.env
.env.*
!.env.example

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg
*.nav
*.snm
*.vrb

# Go build and test artifacts
bin/
dist/
coverage.out
cover.out
*.test

# Python cache and virtual environments
__pycache__/
*.py[cod]
.python-version
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Locust generated artifacts --- regenerated by make targets.
# The hot-account results (referenced by the Experiment 1 writeup) are kept.
load-tests/accounts.json
load-tests/results_exceptions.csv
load-tests/results_failures.csv
load-tests/results_stats.csv
load-tests/results_stats_history.csv
load-tests/report.html

# Terraform artifacts --- state is local-only, tfvars carries account id.
# The lock file (.terraform.lock.hcl) is committed intentionally.
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/terraform.tfvars

# Misc
md-files/
